import DefaultTheme from 'vitepress/theme'
import './custom.css'

// Extends the stock VitePress theme; custom.css repaints its brand/bg vars to
// the app's deep-midnight + sakura palette so the embedded /docs iframe matches.
export default DefaultTheme
